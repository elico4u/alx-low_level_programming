#include "main.h"

/**
 * print_numbers - prints nums between 0 to 9
 * Return: Always 0
 */

void print_numbers(void)
{
	int ch;

	for (ch = 0; ch < 10; ch++)
	{
		_putchar(ch);
	}
	_putchar('\n');
}
